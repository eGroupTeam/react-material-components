version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.15.0
    steps:
      - build
      - run: yarn build
  test:
    docker:
      - image: circleci/node:10.15.0
    steps:
      - test
      - run: yarn test
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test