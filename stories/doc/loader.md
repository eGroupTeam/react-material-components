A wrapper to position the loader component and we use flex to implement it.
Please read implement detail below.

```javascript
  {
    root: {
      display: 'flex',
      justifyContent: 'center'
    },
    stretch: {
      alignItems: 'stretch'
    },
    center: {
      height: '100%',
      alignItems: 'center'
    }
  }
```