import React from 'react';

console.error(
  `@e-group/react-material-components: PageLoader component will be deprecated in the next major release.
  Please migrate to Loader component.`
);

const PageLoader = ({ classes }) => <div />;

export default PageLoader;
